# botc-translations

Translations for Blood on the Clocktower tooling, one JSON file per locale.

- `app/` interface strings
- `game/` character and game text
- `script/` script tool strings

Edit the file for your locale and open a pull request; `en.json` is the source.
